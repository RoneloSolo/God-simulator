#version 330
in vec2 fragTexCoord;
in vec4 fragColor;

uniform vec2 c; 

out vec4 finalColor;


void main(){

    // vec4 color = vec4(1.0);
    // if(fragTexCoord.x * 1920 == c.x && fragTexCoord.y * 1080 && c.y) color = vec4(1.0,1.0,1.0,1.0);
    // else color = vec4(0.0,0.0,0.0,1.0);

    // finalColor = color;
}